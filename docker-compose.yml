services:
  bitbites-fe:
    image: ${REPO}/bitbites-fe
    build:
      context: .
    env_file:
      - .env
    networks:
      - bites-api
    ports:
      - 8080:8080

networks:
  bites-api:
    external: true
